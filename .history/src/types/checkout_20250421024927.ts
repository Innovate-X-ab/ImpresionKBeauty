export interface ShippingDetails {
  firstName: string;
  lastName: string;
  phone: string; // Make sure phone is included and required
  email: string;
  address: string;
  city: string;
  postCode: string;
  country: string;
}